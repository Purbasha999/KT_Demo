# Platform DB
PLATFORM_DB_HOST=localhost
PLATFORM_DB_PORT=3306
PLATFORM_DB_USER=root
PLATFORM_DB_PASSWORD=
PLATFORM_DB_NAME=KT_PLATFORM

JWT_SECRET=MY_JWT_KT_SECRET

LLM_API_URL=https://llm.voxomos.ai/gpt/chatCompletion
LLM_API_TOKEN=94f52d5ae0ce52d1cace2d51a51296e88b8f7b02
LLM_MODEL=gpt-4o

# Encryption key for client DB credentials
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=Cx2-VjVfO1rx7coalYl4C9SrQse5axNdjN5LG1l8tzc=

# RAG — Embeddings (voxomos API)
EMBEDDING_API_URL=https://llm.voxomos.ai/gpt/embedding
EMBEDDING_API_TOKEN=94f52d5ae0ce52d1cace2d51a51296e88b8f7b02
EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_DIMENSIONS=3072

# RAG — Qdrant (run: docker run -p 6333:6333 qdrant/qdrant)
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=          # only needed for Qdrant Cloud
QDRANT_COLLECTION=kt_vox_documents

# RAG — Ingestion
CHUNK_SIZE=500
CHUNK_OVERLAP=150

# RAG — Retrieval
RAG_TOP_K=8
RAG_SCORE_THRESHOLD=0.2

# Upload limit (MB)
MAX_UPLOAD_SIZE_MB=50

# Super Admin credentials
SUPERADMIN_LOGIN=ADMIN
SUPERADMIN_PASSWORD=ADMIN

